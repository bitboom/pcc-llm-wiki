# Summaries

This directory is reserved for temporary or intermediate outputs that are useful during ingestion but not yet filed into the long-lived wiki.

Examples:

- first-pass source summaries
- comparison tables generated during a query
- working notes that may later become `wiki/research/` pages
